# Base URL of the SDOC FastAPI backend, with no trailing slash.
# Example: https://api.example.com
VITE_API_BASE_URL=

# "true" serves the in-memory demo dataset and never contacts the backend.
# Set to "false" once VITE_API_BASE_URL points at a running backend.
VITE_USE_MOCK=true
